import 'package:flutter/material.dart';


class AppColors {
  static const background = Color.fromRGBO(15, 18, 18, 1);       // Deeper terminal black
  static const sideNav = Color.fromRGBO(24, 28, 28, 1);          // Dark obsidian container
  static const searchBar = Color.fromRGBO(24, 28, 28, 1);        // Matching slick input fill
  static const searchBarBorder = Color.fromRGBO(0, 255, 136, 0.3); // Faint glowing green border
  static const iconGrey = Color(0xFF00FF88);                      // Vivid matrix green for icons
  static const textGrey = Color(0xFFE0E6ED);                      // Sharp, crisp off-white for main text
  static const footerGrey = Color(0xFF5A7365);                    // Deep matrix digital dim green
  static const proButton = Color.fromRGBO(35, 43, 39, 1);         // Dark gunmetal with subtle green tint
  static const cardColor = Color(0xFF1E2424);                     // Elevated tactical terminal card
  static const submitButton = Color.fromRGBO(0, 255, 136, 1);     // Hyper-vibrant toxic green action button
  static const whiteColor = Colors.white;
}

// class AppColors {
//   static const background = Color.fromRGBO(10, 15, 30, 1);        // Deep midnight cosmic blue
//   static const sideNav = Color.fromRGBO(16, 22, 42, 1);           // Rich cyber navy dock
//   static const searchBar = Color.fromRGBO(16, 22, 42, 1);         // Clean, matching input field
//   static const searchBarBorder = Color.fromRGBO(0, 217, 255, 0.4); // Electric neon blue outline
//   static const iconGrey = Color(0xFF00D9FF);                      // High-visibility cyan for interactive icons
//   static const textGrey = Color(0xFFE2E8F0);                      // Crisp, bright silver-white text
//   static const footerGrey = Color(0xFF64748B);                    // Muted deep-space slate text
//   static const proButton = Color.fromRGBO(255, 0, 127, 1);        // Electric hot magenta to draw upgrade clicks
//   static const cardColor = Color(0xFF1E293B);                     // Lighter cyber-slate container card
//   static const submitButton = Color.fromRGBO(0, 217, 255, 1);     // Vibrant electric cyan execution button
//   static const whiteColor = Colors.white;
// }

